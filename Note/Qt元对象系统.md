# Qt元对象系统

Qt实质上是一个跨平台的C++开发类库，为开发GUI应用程序和非GUI应用程序提供了各种类。

Qt对标准C++进行了扩展。引入了一些新的概念和功能。Qt的元对象编译器（Meta-Object Compiler）是一个预处理器，将源代码中的Qt特性转换为标准C++兼容的形式，然后再交给C++编译器进行编译。

> Qt Core模块是Qt类库的核心，所有其他的模块都依赖于此模块，如果使用qmake来构建项目，Qt Core模块是被自动加入的，




